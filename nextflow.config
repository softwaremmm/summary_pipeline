profiles {
    docker {
        enabled = true
        temp = 'auto'
    }
    local_docker {
        docker {
            enabled = true
        }
        // Automated testing will need to create container with this name
        params.test_container = "summary_test"
    }
}